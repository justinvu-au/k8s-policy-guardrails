apiVersion: v1
kind: Pod
metadata:
  name: bad-no-label-pod
  namespace: default
spec:
  containers:
    - name: nginx
      image: nginx
      resources:
        limits:
          cpu: "200m"
          memory: "256Mi"